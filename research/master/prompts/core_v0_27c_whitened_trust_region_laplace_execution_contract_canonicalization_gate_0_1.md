# MASTER Prompt — CORE v0.27c Whitened Trust-Region Laplace Execution Contract Canonicalization Gate 0.1

## Purpose

Canonicalize and freeze the complete pre-execution contract for the single v0.27c follow-up route authorized by RB-016. Do not generate or inspect any confirmatory `2711xxx` stochastic dataset and do not claim redesigned-method performance.

## Mandatory authorities

Read and obey at minimum:

- `PROJECT_GOVERNANCE.md`
- `research/core/STATUS.md`
- `research/master/STATUS.md`
- `research/master/project_status.md`
- `research/master/decision_branch_log.md`
- RB-013 v0.27 scope freeze
- RB-014 v0.27 execution contract freeze
- RB-015 v0.27 scientific FAIL result freeze
- RB-016 v0.27b diagnosis/follow-up scope freeze
- `research/core/v0_27b_inference_failure_mechanism_followup_preregistration_gate_0_1.md`.

Use frozen work as premises. Do not re-derive frozen science merely for context.

## Immutable scientific boundary

RB-015 remains `SCIENTIFIC FAIL` and may not be relabelled, rescued or overwritten.

The RB-015 `2701xxx` datasets are DEVELOPMENT ONLY. No confirmatory performance metric may use them.

The reserved disjoint `2711xxx` seed namespace is UNGENERATED / UNEVALUATED at gate start and must remain so throughout this contract gate.

## Single allowed route

Canonicalize only the `v0.27c Whitened Trust-Region Laplace Route` frozen in RB-016:

- whitened trial variable `u=S(lambda)^(-1)z`;
- exact augmented least-squares trial-mode objective;
- deterministic trust-region Gauss-Newton / nonlinear-least-squares solver class;
- exact current-chart JAX Jacobians;
- physical rerecording of every proposal;
- physically/chart-invalid proposals rejected by radius contraction/retry;
- inherited Laplace hierarchy, outer bounded-Powell class, three starts, scientific model/design and RB-014 scientific thresholds.

No solver-family search is authorized.

## Required contract freeze list

Before any effect-bearing output, specify numerically and completely:

1. exact trust-region algorithm and step computation;
2. initial, minimum and maximum trust radii;
3. predicted/actual reduction definitions;
4. acceptance ratio thresholds;
5. radius contraction/expansion factors and update logic;
6. treatment of physically invalid, colliding, nontransverse or derivative-chart-invalid proposals;
7. stationarity/convergence criteria in whitened coordinates;
8. maximum trust-region iterations;
9. residual/Jacobian/physical-rerecording evaluation budgets;
10. maximum invalid-proposal contractions/retries;
11. deterministic tie/failure semantics;
12. exact final Hessian/logdet computation and coordinate transformation semantics;
13. treatment of a failed trial mode within an outer point;
14. exact outer-start assembly and bounded-Powell behavior inherited from or prospectively clarified relative to RB-014;
15. deterministic TR-27C-01 through TR-27C-07 fixtures, tolerances, negative controls and PASS/FAIL rules;
16. the disjoint `2711xxx` 32-replicate seed mapping exactly as frozen in RB-016;
17. all inherited RB-014 physical/statistical design constants and scientific thresholds;
18. environment-capture and rerun-on-environment-change rule;
19. anti-cherry-picking and software-plumbing correction rule;
20. explicit exclusions/deferred branches.

## TR-27C validation minimum

The finite deterministic suite must include exactly the conceptual cases preregistered in RB-016:

- TR-27C-01 whitening algebra identity;
- TR-27C-02 exact linear-Gaussian mode;
- TR-27C-03 actual Lighthouse residual/Jacobian transformation;
- TR-27C-04 trust-region invalid-step contraction;
- TR-27C-05 stationarity and objective consistency;
- TR-27C-06 deterministic development regression subset including `d=5/S2/r0` and `d=9/S2/r0`, development-only;
- TR-27C-07 finite outer-start assembly.

Every tolerance and finite fixture must be fixed before execution. The old development count `94/96` may not be used as a success threshold.

## Forbidden

- no `2711xxx` RNG creation;
- no confirmatory dataset generation;
- no confirmatory v0.27c solver execution;
- no tuning after TR-27C or scientific output;
- no solver-family comparison/search;
- no RB-015 relabelling;
- no change to graph, truth, R, H, timing noise, missingness or hierarchical family;
- no active pulse/probe extension;
- no EM/VI/MCMC substitute;
- no legacy C3 promotion;
- no v0.28;
- no applications, novelty positioning or manuscript claims.

## Required output

Create:

`research/core/v0_27c_whitened_trust_region_laplace_execution_contract_canonicalization_gate_0_1.md`

It must contain Git/environment identity, complete frozen numerical contract, TR-27C-01…07 definitions, inherited-vs-new table, immutable seed boundary, exact execution decision hierarchy, anti-cherry-picking/change control, exclusions, overall gate decision, proposed MASTER freeze and STOP boundary.

## Gate decision

PASS only if the complete pre-execution v0.27c contract is finite, reproducible and freeze-ready with all required constants/tolerances/budgets fixed before confirmatory output.

CONDITIONAL/BLOCKED if a genuine completeness defect prevents a freeze without inspecting confirmatory data.

No scientific PASS/FAIL is possible in this gate because confirmatory v0.27c science is forbidden.

After writing the result, update `research/core/STATUS.md` to `RETURN TO MASTER` or `BLOCKED` and stop. Do not self-authorize execution.
